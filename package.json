{
  "name": "neo4j-graphql",
  "version": "1.0.0",
  "description": "This is a prototype to prove 2 things:",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd query-api && npm i",
    "db": "docker-compose up -d",
    "seed": "cd seed && npm start && cd -",
    "start:query-api": "cd query-api && npm start",
    "start": "npm run db && scripts/wait-for-neo4j && concurrently \"npm:start:query-api\"",
    "clean": "docker-compose down; sudo rm -rf ./data"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.5.1"
  }
}
